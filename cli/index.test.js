const expect = require("chai").expect;
const app = require("./index");

describe("Invest-Report App", function() {
    it("should perform basic calculation", function() {
        expect(1 + 1).to.equal(2);
    });
});
